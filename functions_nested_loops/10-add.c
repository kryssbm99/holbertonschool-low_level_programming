#include "main.h"
/**
 *add - Program adds two int
 *
 *@r: first character that is being checked
 *@n: second character that is being checked
 *Return: Returns sum of both int
 */
int add(int r, int n)
{
int x;
x = r + n;
return (x);
}
