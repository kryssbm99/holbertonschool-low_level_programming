#include "main.h"
/**
 *_abs - Program checks for absolute value
 *
 *@r: character that is being checked
 *
 *Return: Returns 0
 */
int _abs(int r)
{
	if (r < 0)
	{
	r = -r;
	return (r);
	}
		else
		{
		return (r);
		}
			return (0);
}
